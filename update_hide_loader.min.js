const e=require("fs"),i=require("path"),n=e.readdirSync(__dirname).filter(e=>e.endsWith(".html")&&!e.endsWith("-static.html"));for(const d of n){let n=e.readFileSync(i.join(__dirname,d),"utf8");n.includes("const hideInitialLoader = () => {")&&!n.includes("window.__loaderTimeout")&&(n=n.replace("const hideInitialLoader = () => {","const hideInitialLoader = () => {\n          if (window.__loaderTimeout) clearTimeout(window.__loaderTimeout);"),e.writeFileSync(i.join(__dirname,d),n,"utf8"),console.log(`Updated hideInitialLoader in ${d}`))}