const fs = require('fs'); const files = fs.readdirSync(__dirname).filter(f => f.endsWith('.html')); for (const file of files) { let content = fs.readFileSync(file, 'utf8'); if (content.includes('rgba(255, 255, 255, 0.95)')) { content = content.replace(/background:\s*rgba\(255,\s*255,\s*255,\s*0\.95\);/g, 'background: transparent;'); fs.writeFileSync(file, content); console.log('Updated ' + file); } } console.log('Done');
